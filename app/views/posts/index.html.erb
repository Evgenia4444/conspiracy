

<div class="posts">


<div class="posts_container">

<% @posts.each do |post| %>
 <div class="post_container">
    <div class="post_data">
      <div class="user_data">
        
      </div>
      <img src="../../assets/images/more_icon.png" alt="">
      <p><%= link_to 'Редактировать', edit_post_path(post) %></p>
      <p> <%= link_to 'Удалить', post, method: :delete, data: { confirm: 'Вы уверены, что хотите удалить пост?' } %></p>
    </div>
    <div class="post_content">
      <p><%= post.content %></p>
      <%= image_tag(post.image.url, alt: 'Image') if post.image? %>
    </div>

        <td>
	      <%= post.likes.count %> <%= (post.likes.count) == 1 ? 'Like' : 'Likes'%>
        </td>
        <td>
	<%= post.commentts.count %> <%= (post.commentts.count) == 1 ? 'Comment' : 'Comments'%>
</td>
    </div>


    <%# <div class="comments">

    <div class="comments_action">
        <%# <%= render "commentts/form" %> 
        <%# <button class="comment_publish"></button>
    </div> %>

    <%# <div class="comments_list">
        
    </div>

    </div>  %>


        <% end %>
    <%= link_to 'Создать пост', new_post_path, :class => 'createpost_btn' %>
    
</div>

</div>
   
</section>



